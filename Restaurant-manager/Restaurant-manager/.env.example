DJANGO_SECRET_KEY=change-me-for-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

DB_ENGINE=mysql
DB_NAME=Restaurant
DB_USER=root
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306

# False = Django maps the existing SQL schema without creating these tables.
# True = Django migrations may manage the restaurant app tables.
RESTAURANT_MODELS_MANAGED=False

